#!/bin/bash

#
# ws: set up NODE_PATH for use in this workspace
#

cd $(dirname $0)
export $(./npath)
export AMQP_HOST=10.99.99.5	# only used for COAL
export PATH=$PWD/../deps/node-install/bin:$PWD/../deps/ctf2json:$PATH
export SRC=$PWD/../
cd - > /dev/null
exec $SHELL
